{
  "name": "sqlite-wiki",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "start": "npm run dev",
    "build": "vite build",
    "preview": "vite preview",
    "fix": "npx prettier . --write",
    "deploy": "npm run fix && npm run build && git add . && git commit -a && git push"
  },
  "devDependencies": {
    "vite": "^4.3.8"
  },
  "dependencies": {
    "@sqlite.org/sqlite-wasm": "^3.42.0-build1",
    "marked": "^5.0.2",
    "prettier": "^2.8.8",
    "simpledotcss": "^2.2.1"
  }
}
